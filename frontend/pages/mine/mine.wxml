<view class="page">
  <view class="profile-card">
    <image src="{{userInfo.avatarUrl}}" class="avatar"></image>
    <view class="profile-meta">
      <text class="name">{{userInfo.nickname || userInfo.name || '未设置姓名'}}</text>
      <text class="student-id">{{userInfo.studentId || '未设置学号'}}</text>
      <text class="detail">{{userInfo.college || '未设置学院'}}</text>
      <text class="detail">{{userInfo.className || '未设置班级'}}</text>
      <text class="detail">{{userInfo.phone || '未设置手机号'}}</text>
    </view>
    <view class="edit-button" bindtap="onEditProfile">
      <text>编辑资料</text>
    </view>
  </view>

  <view class="section">
    <view class="section-header">
      <text class="section-title">我的票根</text>
      <text class="section-more" bindtap="onViewAllTickets">查看全部</text>
    </view>

    <view class="ticket-list">
      <view
        wx:for="{{tickets}}"
        wx:key="id"
        class="ticket-card"
        bindtap="onTicketTap"
        data-ticket-number="{{item.ticketNumber}}"
      >
        <image wx:if="{{item.coverImageUrl}}" src="{{item.coverImageUrl}}" class="ticket-cover" mode="aspectFill"></image>

        <view class="ticket-header">
          <text class="ticket-title">{{item.activityTitle}}</text>
          <text class="ticket-status {{item.status === 'valid' ? 'valid' : 'used'}}">
            {{item.status === 'valid' ? '有效' : '已使用'}}
          </text>
        </view>

        <view class="ticket-info">
          <view class="info-row">
            <text class="info-label">时间</text>
            <text class="info-value">{{item.time}}</text>
          </view>
          <view class="info-row">
            <text class="info-label">地点</text>
            <text class="info-value">{{item.location}}</text>
          </view>
          <view class="info-row">
            <text class="info-label">座位</text>
            <text class="info-value">{{item.seatNumber}}</text>
          </view>
        </view>

        <view class="ticket-qr">
          <image src="{{item.qrCode}}" class="qr-code"></image>
          <text class="ticket-number">{{item.ticketNumber}}</text>
        </view>
      </view>

      <view wx:if="{{tickets.length === 0}}" class="empty">
        <text>暂无票根，先去首页看看活动吧</text>
      </view>
    </view>
  </view>

  <view class="button-row">
    <button class="secondary-button" bindtap="onGoToAdmin">切换身份</button>
    <button class="danger-button" bindtap="onLogout">退出登录</button>
  </view>
</view>
